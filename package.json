{
  "name": "@meskill/sharec-config",
  "version": "1.0.5",
  "main": "index.js",
  "license": "MIT",
  "files": [
    "configs"
  ],
  "scripts": {
    "postinstall": "sharec && cd $PROJECT_CWD && yarn config set nmMode hardlinks-global && yarn config set enableGlobalCache true && yarn plugin import typescript && yarn dlx @yarnpkg/sdks vscode"
  },
  "packageManager": "yarn@3.0.2",
  "dependencies": {
    "sharec": "^2.10.7"
  }
}
